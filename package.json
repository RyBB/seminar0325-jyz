{
  "name": "seminar0325-jyz",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@kintone/rest-api-client": "^2.0.34",
    "@twilio/runtime-handler": "1.2.2",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.4.1"
  },
  "engines": {
    "node": "12"
  }
}
